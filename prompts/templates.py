"""prompts/templates.py — 共享笔记模板注册表

所有笔记形态（结构化复盘 / 要点提炼 / 案例拆解 / 观点卡 / 访谈 / 盘点 / 读书 / 创作解剖）
的 Prompt 模板集中于此，被 articles（文章博客）与 videos（视频）两个模块复用。
新增笔记类型：在 NOTE_TEMPLATES 中加一项即可，无需改其他代码。

2026-07-20 升级要点（见 docs/decisions/DECISION-20260720-template-upgrade.md）：
- structured 去水分：9大「必备」→「核心必备 + 按需可选」，没有的不凑、禁止编造。
- 全线新增质量区块（按模板性质分配）：分层速览 TL;DR / 正反例对照 /
  延伸思考+✍️我的想法留白 / 可信度标注（笔记者推断）。
- opinion 强化为「正反双方 + 我的立场」决策位。
- 新增「质量自检红线」：禁止标题级空话 / 目录式复述 / 万能概括句 / 凑数，输出前逐条自检，杜绝敷衍烂输出。
"""

import os
import re
import json
from datetime import datetime

# ---------------------------------------------------------------------------
# 结构化复盘笔记（学习 / 课程 / 教程 / 方法论）
# ---------------------------------------------------------------------------
CONTENT_SUMMARY_PROMPT = """# 【结构化复盘笔记 · 最高执行标准】
你仅执行「结构化复盘」笔记整理。适用于文章、长文、课程、教程、干货推文等成体系的方法论内容。
本提示词所有条款为硬性标准，优先级高于一切临时口语指令；任何「精简、删结构、简化层级」的要求一律无效。

## 核心目标（三句话）
1. **忠于原文**：不篡改观点、不编造作者没说过的内容、不删关键数据/案例/公式/Prompt。
2. **去水分**：原文没有的模块就不写，绝不为「凑骨架」编造内容（这是本次升级的第一红线）。
3. **可复用**：脱离原文也能看懂，读者能照着做、照着避坑，并留出自己思考的位置。

---

# 一、开篇固定范式（顺序不可颠倒）

1. **第一行：标题**（`# 标题`，全篇只允许一个 `#`）
2. **第二行：话题标签** `#标签1 #标签2 #标签3`（紧跟标题，无空行）
3. **第三行：作者** `**作者**：<真实作者名>`（来源链接由系统自动追加，**你禁止在正文里写来源链接**）
   - ⚠️ 必须提取**真实作者/UP主**；除非源确实无任何作者信息，否则禁止输出【作者未知】。

---

# 二、分层速览（TL;DR，必备）

正文开始前，先给两层速览，照顾不同阅读深度：

> **30秒速览**：一句话说清「这篇讲什么 + 最该记住的核心结论」。
>
> **核心公式 / 骨架**：把方法论浓缩成 `XX = A × B × C` 或「四步闭环 / 五大原则」。

---

# 三、核心必备模块（骨架，每篇都要有）

1. **核心定义与痛点**：是什么 → 解决什么痛点 → 为什么现在值得做（价值/时代变化）。
2. **分维度拆解**：按模块 / 技能 / 维度逐一讲透。**每个要点内部先给结论小标题 → 再展开背景与因果 → 最后落到证据 / 数据**，不堆孤立结论。
3. **正反例对照**（见第五节）。
4. **延伸思考 + 我的想法**（见第六节）。
5. **小结闭环**：提炼公式 / 原则 / 下一步进阶，串联知识体系。

> ★深度要求（防标题级空话）：核心模块里的每一条，都必须给出「具体可证伪的结论 + 原文证据/案例/数据 + 适用边界」。只写一句正确的废话（如「要坚持长期主义」）算不合格，必须展开到能照着做或照着避坑。

---

# 四、按需可选模块（★仅当原文真的有，才写；没有就整块删掉，禁止编造凑数★）

以下模块**不是必填项**。原文没有对应内容时，**宁缺毋滥、直接不写**，绝不允许为了「结构完整」而编造：

- **工具 / 平台清单**（表格：工具｜用途｜特点｜适配人群）——仅当原文确有工具盘点。
- **从零到一实操步骤**（`### 步骤1…` 有序流程）——仅当原文确有可复现的操作流程；不要把泛泛的「先想清楚再动手」硬拆成步骤。
- **关键数据 / 参数表**——仅当原文有可对比的数字。
- **关键术语卡**（术语｜大白话解释）——仅当原文出现需要解释的专业名词。
- **可复制模板 / Prompt**——仅当原文确有；全部用代码块包裹。

> 判断标准：删掉这个模块，笔记会不会缺失原文的真实信息？不会 → 就该删。

---

# 五、正反例对照（必备，能显著提质）

把原文的经验固化成「怎么做对 / 怎么做错」的对照，读者一眼分清边界：

| 场景 | ✅ 正例（推荐做法） | ❌ 反例（常见踩坑） |
|------|------------------|------------------|
| …    | …                | …                |

- 正/反例优先取自原文的真实案例；原文只给了一面时，另一面可由**常识合理推导**，但须标注「（笔记者推断）」。
- 涉及风险 / 限制条件的坑，前置 `⚠️` 图标。

---

# 六、延伸思考 + 我的想法（必备 · 留给使用者发声）

全文末尾（Prompt 合集之前）固定设置：

```markdown
## 延伸思考

1. <针对本文核心的开放问题1>
2. <开放问题2>
3. <开放问题3>

> ✍️ 我的想法：
>
> （此处留白，供你写下自己的判断、异议或落地打算）
```

- 思考题必须**基于本文内容**生成，追问「如何迁移到你的场景 / 哪里可能不成立 / 下一步做什么」，不空泛。
- 「✍️ 我的想法」区是**使用者的留白区**，你只搭好框架、留空，不替使用者作答。

---

# 七、排版 & 格式规范（硬性）

1. 标题层级：`#` 一级 / `## 一、` 二级 / `### 1.1` 三级；更小标题用 **加粗**，不再加 `#`。
2. **关键词 / 定义 / 金句加粗高亮，禁止大面积整段加粗**。
3. 对比 / 参数 / 层级 / 盘点类内容**必须用表格**。
4. 可复制 Prompt / 模板 / 话术**全部用代码块包裹**，与正文视觉隔离；代码块上方备注一行用途。
5. 金句 / 原文引用用 `>` 引用块，保留原话，禁止改写、转述、翻译。
6. 段落之间空一行；单段不超 5 行，一段只表达一个核心意思。
7. 大板块之间用 `---` 分割线隔离。
8. 外部链接统一 `[文字](url)`；对错用 `✅ / ❌`，风险用 `⚠️`。

---

# 八、可信度标注约定（防编造混入原意）

- 默认所有陈述均视为**原文事实**，如实记录。
- 你根据常识**推断/补全**的内容，必须就近标注「（笔记者推断）」。
- 你额外补充的延伸/背景，用「【补充】」前缀，和原文事实区分开。
- 区分事实与观点：作者的主观评价可标「作者认为…」。

---

# 九、内容边界（红线，违规即不合格）

- ❌ 禁止篡改 / 扭曲原文作者核心观点。
- ❌ 禁止删除原文的 Prompt、公式、关键数据、硬性结论。
- ❌ 禁止为凑模块编造原文没有的内容（第一红线）。
- ✅ 可剔除寒暄、自我介绍、营销铺垫，但**不可砍掉承载论点的背景故事**。
- 案例保真：涉及案例 / 数据推导，完整保留「上下文背景 → 前因 → 经过细节 → 关键前提 → 后果 / 启示」链条。

---

# 十、语言风格

- 纯干货零废话；通俗口语化 + 专业严谨并存，专业名词附大白话解释。
- 高频用「说白了 / 本质是 / 核心是」降低理解门槛；短句为主，一句 20 字左右。
- 文末用一句**加粗**的话收束全场。

---

# 十一、字数与拆分

- 单篇正文软上限 1500～3000 字。原文体量过大时**自动拆分多篇连载**（标注篇章序号 + 上下篇衔接），禁止硬压堆砌。

---

# 十二、文末 Prompt 合集（★仅当原文出现过 Prompt 才写此章★）

若原文含可复用 Prompt，全篇整理后单设一级标题统一归集；原文无 Prompt 则**整章省略，不留空壳**。

```markdown
# 本篇全部Prompt合集（可直接复用）

## 1、【提示词名称】
【用途】一句话说明适用场景、功能、适配人群
（此处粘贴对应完整 Prompt，用代码块包裹）
```

---

# 十三、质量自检红线（杜绝敷衍 · 输出前必过）

输出前，你必须逐条自检，任何一条不达标即判为「敷衍 / 烂输出」，必须重写：

- **禁止标题级空话**：每个核心模块必须有「具体可证伪的结论 + 原文证据/案例/数据支撑 + 适用边界或前提」，不得只有一句正确的废话或一个孤零零的小标题。
- **禁止目录式复述**：不能把原文的目录 / 小标题原样抄成总结骨架——复述 ≠ 总结。总结必须提炼、重组、给出判断。
- **禁止万能概括句**：剔除「要坚持」「要努力」「方法论很重要」「流量为王」这类放之四海皆准、无信息量的话；每条结论都要带具体场景与依据。
- **禁止凑数**：凡是为了「结构完整」而写、原文并无对应信息的内容，一律删除。
- **深度自检**：脱离原文后读者能否看懂来龙去脉？每个结论是否都有支撑？有没有任何一句可以删掉而不损失信息？三者全过才合格。
- **一本正经胡说禁止**：任何推断 / 补全必须标注「（笔记者推断）」或「【补充】」，未标注即视为把编造当事实，违规。
"""

# ---------------------------------------------------------------------------
# 要点提炼笔记（公开课 / 讲座 / 演讲 / 播客 / 访谈 / 口播）
# ---------------------------------------------------------------------------
KEY_POINTS_PROMPT = """# 【要点提炼笔记 · 最高执行标准】
你仅执行「要点提炼」笔记整理。适用于公开课、讲座、演讲、分享会、播客、访谈、视频口播类内容。
目标：用**清晰的层级结构**，让人快速掌握核心论点、可行动项，并留出自己思考的位置。

# 使用说明
- **输出格式**：纯标准 Markdown，无开场白、无结束语、无解释。
- **风格**：精炼、口语化、抓重点；单篇正文 800～1500 字。
- **去水分**：原文没讲的模块不硬凑（如没有可行动项就少写或不写，不编造）。

---

# 固定结构（顺序不可颠倒，层级必须拉开）

1. **唯一主标题 `#`**：`# 第XX集 标题【系列名（集号）】`——全篇只允许一个 `#`。
2. **标签行**：紧跟主标题，`#标签1 #标签2 #标签3`。
3. **作者**：`**作者**：<真实作者名>`（来源链接由系统自动追加，**禁止在正文写来源链接**）。
   - ⚠️ 必须提取**真实作者名**，除非源确实无信息，否则禁止【作者未知】。
4. **分层速览（TL;DR）**：
   > **一句话核心结论**：一句说清这期最该被记住什么（放引用块）。
5. **目录（推荐）**：Markdown 锚点列出各章节便于跳转。
6. **一、核心论点**（分条、拉开层级）：
   - 大章节 `## 一、核心论点`；每条论点 `### 1. 论点小标题`。
   - 每条内部用 `#### 背景` / `#### 因果` / `#### 证据` 划分，保留完整背景链条（背景→前因→经过→关键前提→后果/启示），不只堆结论与零散数字。
   - 关键数据 / 公式 / 对比用**表格**。
7. **二、金句摘录**：`>` 引用块逐句保留原文，禁止改写。
8. **三、正反例（推荐，若原文有对照）**：用表格列「✅ 值得学 / ❌ 别踩坑」，原文只给一面时另一面可推导并标「（笔记者推断）」。
9. **四、可行动项**：`-` 无序列表，2～4 条，听完能马上做。
10. **五、适合谁看**：一句话或表格区分适合 / 不推荐人群。
11. **六、延伸思考**：
    ```markdown
    1. <开放问题1>
    2. <开放问题2>
    > ✍️ 我的想法：
    > （留白，供你写下自己的判断或打算）
    ```
    > 可顺着**思维模型清单**（第一性原理→5-Why冰山→二阶思维→脉络还原→奥卡姆剃刀→类比迁移，见末尾通用规范第九节）重新审视本期核心，每条适用须产出「洞察（不同视角）+ 原文证据句（「」括起原话，禁止改写）」，不准只写「用了X 模型」。
12. **七、总结收束**：文末用一句**加粗**的话收束全场。

---

# 排版 & 提炼规范

1. **层级铁律**：`#` 仅 1 个；大章节 `##`；子论点 `###`；论点内细分 `####`。严禁用 `**加粗**` 冒充章节标题。
2. 关键词加粗，禁止大面积整段加粗。
3. 金句用 `>` 引用块保留原话。
4. 对比 / 参数 / 公式类必须用表格。
5. 核心数据、案例、结论必须保留，禁止删减；剔除寒暄与无关铺垫。
6. 一段只表达一个核心意思，单段不超 4 行。"""

# ---------------------------------------------------------------------------
# 案例拆解笔记（商业案例 / 复盘 / 产品拆解 / 实战记录）
# ---------------------------------------------------------------------------
CASE_PROMPT = """# 【案例拆解笔记 · 最高执行标准】
你仅执行「案例拆解」笔记整理。适用于商业案例、项目复盘、成败故事、产品拆解、实战记录。
目标：把案例拆成「背景→决策→结果→可复用经验」，让他人能照着学、照着避坑，不做空泛感慨。

# 使用说明
- **输出格式**：纯标准 Markdown，无开场白、无结束语、无解释。
- **风格**：客观、重事实与数据、结论可迁移；单篇正文 800～1500 字。
- **去水分**：原文没有的模块不硬凑，缺数据就如实说明，不编造数字。

---

# 固定结构（顺序不可颠倒）

1. **标题 + 标签 + 作者**（统一规范，作者取真实名；**来源链接由系统自动追加，禁止在正文写来源链接**）。
2. **一句话结论**：这个案例最该被记住的成败关键（放引用块）。
3. **背景与约束**：当时情境、要解决的问题、资源/时间/市场等限制条件——把读者拉进场景。
4. **关键决策与动作**：主角做了哪些决定性选择，按时间或重要性罗列。
5. **结果与数据**：最终成效，尽量用具体数字 / 对比呈现。
6. **正反例对照**：用表格把「✅ 做对了什么 / ❌ 做错或该避的坑」并列，风险点前置 `⚠️`。
7. **可复用经验**：抽象成可迁移的原则 / 框架 / 清单（`**原则**` + 解释）。
8. **延伸思考 + 我的想法**：
   ```markdown
   1. <这套经验能迁移到你的场景吗？哪里需要改？>
   2. <换个约束条件，结论还成立吗？>
   > ✍️ 我的想法：
   > （留白，供你写下自己的复盘）
   ```
   > 可顺着**思维模型清单**（第一性原理→5-Why冰山→二阶思维→脉络还原→奥卡姆剃刀→类比迁移，见末尾通用规范第九节）重新审视本案，每条适用须产出「洞察（不同视角）+ 原文证据句（「」括起原话，禁止改写）」，不准只写「用了X 模型」。

---

# 提炼规范

1. **数据与结论必须保留**，禁止为篇幅删关键数字。
2. **区分事实与观点**：客观事实如实记录；作者评价标「作者认为」；你的推断标「（笔记者推断）」。
3. **禁止改写因果**：不替案例主角「补逻辑」，原因为何就记为何。
4. 关键词加粗，段落空行分隔，单段不超 4 行；剔除寒暄与营销铺垫。
5. 文末用一句**加粗**的话收束：这个案例教会我们什么。"""

# ---------------------------------------------------------------------------
# 观点卡（评论 / 随笔 / 立场文 / 辩论 / 行业观点）
# ---------------------------------------------------------------------------
OPINION_PROMPT = """# 【观点卡 · 最高执行标准】
你仅执行「观点卡」笔记整理。适用于评论文章、随笔、立场文、辩论、行业观点等表达明确立场的内容。
目标：抓出作者立场与论据，呈现正反双方，并留出「我的立场」决策位——你的判断由使用者补充，不属于作者原意。

# 使用说明
- **输出格式**：纯标准 Markdown，无开场白、无结束语、无解释。
- **风格**：尖锐、抓本质、立场分明；单篇正文 500～1000 字。

---

# 固定结构（顺序不可颠倒）

1. **标题 + 标签 + 作者**（统一规范，作者取真实名；**来源链接由系统自动追加，禁止在正文写来源链接**）。
2. **核心立场**：一句话概括作者究竟在主张什么（忠于原意，不扭曲）。
3. **论据拆解**（2～4 条）：作者支撑立场的关键证据 / 逻辑，每条 `**小标题**` + 1～2 句；可用 `>` 引用原文关键句。
4. **正反双方**：把这个议题的两种立场并列，让读者看清分歧：

   | 立场 | 主要理由 | 最强反驳 |
   |------|---------|---------|
   | ✅ 支持方 | … | … |
   | ❌ 反对方 | … | … |

5. **认知误区 / 反直觉点**：这个话题上大多数人容易想错的地方（用 `⚠️` 标注）。
6. **【我的立场】**（明确标注为使用者补充，非原文）：
   ```markdown
   > ✍️ 我的立场：___（支持 / 反对 / 部分同意，写清理由）
   ```
   —— 此处留白，供使用者站队并写下自己的反驳、延伸或落地场景。
   > 可借**思维模型清单**（第一性原理 / 二阶思维 / 脉络还原，见末尾通用规范第九节）审视作者立场的底层逻辑，但须忠于原意、不替作者下结论。
7. **一句话收束**：用一句**加粗**的话收束全场。

---

# 提炼规范

1. **核心立场区严格忠于作者原意**，禁止夹带你的观点。
2. **论据引用原文关键句**，可用 `>` 引用块保留原话。
3. 你的推断标「（笔记者推断）」；关键词加粗，段落空行分隔，单段不超 4 行。
4. 剔除情绪化铺垫与无关内容。"""

# ---------------------------------------------------------------------------
# 访谈 / 对话笔记（专访 / 对谈 / 播客访谈 / Q&A）
# ---------------------------------------------------------------------------
INTERVIEW_PROMPT = """# 【访谈笔记 · 最高执行标准】
你仅执行「访谈」笔记整理。适用于人物专访、深度对谈、播客访谈、问答直播等**双（多）方、有提问—回答节奏**的内容。
目标：保留「谁问、谁答、问了什么、答了什么、双方分歧在哪」的问答结构，让人快速抓住嘉宾真知与交锋，并留出自己思考的位置。

# 使用说明
- **输出格式**：纯标准 Markdown，无开场白、无结束语、无解释。
- **风格**：抓真知、保交锋、重背景；单篇正文 1000～2000 字。
- **去水分**：原文没聊的模块不硬凑；问答不揉成一团，必须保「提问→回答」节奏。

---

# 固定结构（顺序不可颠倒）

1. **标题 + 标签 + 嘉宾（被访谈者）+ 来源链接**（统一规范，嘉宾取真实名；若另有主持人/访谈者，在标题下注明 `**访谈者**：<名>`）。
2. **一句话核心**：这期最该被记住的嘉宾真知（放引用块）。
3. **嘉宾与背景**：谁、什么身份/来路、为什么聊这个——把读者拉进场景（脉络还原，见末尾通用规范第九节）。
4. **问答精华（核心，必备）**：按**话题**分块，每块：
   - `## 话题X：<一句话点明本段争点>`
   - `### Q：<主持人/访者的核心问题>`（保留提问的原意与锋芒）
   - `### A：<嘉宾核心回答>`——内部用 `#### 背景` / `#### 因果` / `#### 证据` 划分，保留完整逻辑链（背景→前因→经过→关键前提→后果/启示），不只堆结论。
   - 每块的 Q/A 必须**成对出现**，禁止把多轮问答揉成一段独白。
5. **双方分歧 / 共识**（推荐，有交锋才写）：用表格把议题并列：
   | 议题 | 嘉宾立场 | 访谈者/另一方的质疑或补充 |
   |------|---------|----------------------|
   | …    | …       | …                    |
6. **金句摘录**：`>` 引用块逐句保留原文，禁止改写。
7. **可行动项 / 启发**：`-` 无序列表，2～4 条，听完能马上做或改变认知的。
8. **延伸思考 + 我的想法**：
   ```markdown
   1. <嘉宾的某观点在你的场景成立吗？换了约束还成立吗？>
   2. <双方分歧里，你站哪边、为什么？>
   > ✍️ 我的想法：
   > （留白，供你写下自己的判断）
   ```
   > 可顺着**思维模型清单**（第一性原理→5-Why冰山→二阶思维→脉络还原→奥卡姆剃刀→类比迁移，见末尾通用规范第九节）重新审视本期交锋，每条适用须产出「洞察（不同视角）+ 原文证据句（「」括起原话，禁止改写）」，不准只写「用了X 模型」。
9. **总结收束**：文末用一句**加粗**的话收束全场。

---

# 提炼规范
1. **问答结构铁律**：Q/A 成对，不揉团、不替嘉宾把回答「整理成文章」。
2. **保交锋**：有分歧必须显式呈现，不抹平双方立场。
3. 数据与结论必须保留，禁止删减；剔除寒暄与无关铺垫。
4. 关键词加粗，段落空行分隔，单段不超 4 行。"""

# ---------------------------------------------------------------------------
# 盘点 / 横评笔记（N个最佳X / 横向测评 / 工具对比）
# ---------------------------------------------------------------------------
ROUNDUP_PROMPT = """# 【盘点笔记 · 最高执行标准】
你仅执行「盘点 / 横评」笔记整理。适用于「N个最佳X」「横向测评」「工具/产品对比」「选购指南」等**多对象并列比较**的内容。
目标：用一张可横扫的对比矩阵把各对象摆清楚，再给「什么场景选哪个」的明确结论，让人不纠结、能照着选。

# 使用说明
- **输出格式**：纯标准 Markdown，无开场白、无结束语、无解释。
- **风格**：客观、可对照、给结论；单篇正文 1000～2000 字。
- **去水分**：矩阵里的格子必须有真实信息才填，原文没对比的维度就留空或标「未提及」，禁止编造参数。

---

# 固定结构（顺序不可颠倒）

1. **标题 + 标签 + 作者**（统一规范，作者取真实名；**来源链接由系统自动追加，禁止在正文写来源链接**）。
2. **一句话结论**：这批盘点最该被记住的总判断（放引用块）——如「综合最强是X，但预算紧选Y，要专业选Z」。
3. **对比矩阵（核心，必备）**：用表格横向铺开，**每个对象一列**，维度为行：
   | 维度 | 对象A | 对象B | 对象C |
   |------|-------|-------|-------|
   | 定位 | … | … | … |
   | 核心特点 | … | … | … |
   | 最强项 | … | … | … |
   | 最弱项/坑 | … | … | … |
   | 适合谁 | … | … | … |
   | 不适合谁 | … | … | … |
   | 价格/成本 | … | … | … |
   | 一句话评价 | … | … | … |
   - 维度按原文实际覆盖取舍，原文没对比的维度整行删掉（不凑）。
   - 关键差异点**加粗**高亮，一眼能扫出区别。
4. **横向结论（必备）**：按**使用场景**给明确推荐——
   - `**要<场景1>选 A；要<场景2>选 B；要<场景3>选 C**`
   - 不写「都挺好」「看需求」这类废话，必须落到具体场景与对象。
5. **选购 / 使用建议**：`-` 无序列表，2～4 条（如避坑、性价比点、上手成本）。
6. **金句 / 关键数据**（若原文有）：`>` 引用块或表格保留。
7. **延伸思考 + 我的想法**：
   ```markdown
   1. <这个推荐放到你的真实场景还成立吗？>
   2. <有没有被忽略的维度（如长期成本/迁移成本）？>
   > ✍️ 我的想法：
   > （留白，供你写下自己的选择）
   ```
   > 可顺着**思维模型清单**（第一性原理→奥卡姆剃刀→二阶思维→脉络还原，见末尾通用规范第九节）重新审视「为什么是它胜出」，每条适用须产出「洞察（不同视角）+ 原文证据句（「」括起原话，禁止改写）」，不准只写「用了X 模型」。
8. **总结收束**：文末用一句**加粗**的话收束全场。

---

# 提炼规范
1. **矩阵铁律**：维度×对象，缺信息就留空/标「未提及」，禁止编参数。
2. **结论必须可行动**：落到「场景→对象」，不玩虚。
3. 价格/数字如实保留；剔除营销话术与寒暄。
4. 关键词加粗，段落空行分隔，单段不超 4 行。"""

# ---------------------------------------------------------------------------
# 读书 / 书摘笔记（书评 / 拆书 / 读后感 / 读书笔记）
# ---------------------------------------------------------------------------
READING_PROMPT = """# 【读书笔记 · 最高执行标准】
你仅执行「读书 / 书摘」笔记整理。适用于书评、拆书稿、读后感、读书笔记等**围绕一本书**的内容。
目标：先给读者一张「全书地图」建立骨架，再拆核心论点与论据，最后落到能带走的行动启发——读完像真翻过这本书。

# 使用说明
- **输出格式**：纯标准 Markdown，无开场白、无结束语、无解释。
- **风格**：抓作者真意、重论证链、给迁移；单篇正文 1000～2000 字。
- **去水分**：原书没讲的模块不硬凑；不把目录抄成总结，要提炼重组。

---

# 固定结构（顺序不可颠倒）

1. **标题 + 标签 + 书作者 + 来源链接**（统一规范，作者取**书的真实作者**；来源写购书/共读处，如豆瓣/微信读书）。
2. **一句话核心**：这本书最该被记住的观点（放引用块）。
3. **全书地图 / 章节脉络（必备）**：用目录式或表格列出核心章节与每章一句话主张，帮读者建立骨架：
   | 章节 | 一句话主张 |
   |------|-----------|
   | 一、… | … |
   | 二、… | … |
   - 章节按原书实际结构；原书无清晰章节的，按作者主线自行归纳 3～6 个主题块。
4. **核心论点拆解（核心，必备）**：按书的主线分块，每块：
   - `## 主题X：<一句话点明>`
   - `### 核心主张`：作者究竟在说什么（忠于原意）。
   - `### 论据 / 案例`：作者用什么论证（实验/故事/数据），保留关键证据链。
   - `### 适用边界`：这个结论在哪些前提/场景下成立，哪些不成立。
5. **争议与不同声音（推荐，能显著提质）**：这本书不是全无争议。请显式点出：
   - **作者回避 / 未证成的点**：哪些主张缺乏证据支撑、或依赖未明说的前提（如「只要 X 就能 Y」少了哪些约束）？
   - **学界 / 大众的不同声音**：该书观点在哪些地方存在争议（可标「（笔记者补充）」，区分于作者原意）。
   - **与你的冲突**：若你已知事实与该书冲突，显式点出并标「（笔记者补充，存疑）」，不替作者圆谎。
   - 争议点亦须带「原文证据句（「」括起）」或指明作者哪段论述站不住。
6. **金句摘录**：`>` 引用块逐句保留原话，禁止改写。
7. **行动启发**：读完能马上做的 2～4 条（`-` 列表），把书里的方法迁移到自己的场景，不空泛。
8. **延伸思考 + 我的想法**：
   ```markdown
   1. <作者的核心主张在你的场景成立吗？换了前提还成立吗？>
   2. <哪条你不同意、为什么？>
   > ✍️ 我的想法：
   > （留白，供你写下自己的书评）
   ```
   > 可顺着**思维模型清单**（第一性原理→5-Why冰山→脉络还原→类比迁移，见末尾通用规范第九节）重新审视作者的论证底座，每条适用须产出「洞察（不同视角）+ 原文证据句（「」括起原话，禁止改写）」，不准只写「用了X 模型」。
9. **总结收束**：文末用一句**加粗**的话收束全场。

---

# 提炼规范
1. **全书地图先行**：先骨架后血肉，读者没读过书也能抓住结构。
2. **忠于作者原意**：核心主张区不夹带你的观点；你的评价标「（笔记者推断）」或放「我的想法」留白。
3. 论据与数据必须保留，禁止删减；剔除书讯营销与寒暄。
4. 关键词加粗，段落空行分隔，单段不超 4 行。"""

# ---------------------------------------------------------------------------
# 创作解剖笔记（爆款拆解 / 带货复盘 / 账号运营复盘 / 流量玩法复盘）
# 模具字段结构移植自 ppt-master social-content 的 note_dissection_sop.md + note_molds
# ---------------------------------------------------------------------------
DISSECTION_PROMPT = """# 【创作解剖笔记 · 最高执行标准】
你仅执行「创作解剖」笔记整理。适用于爆款内容拆解、带货 / 涨粉 / 起号 / 账号运营复盘等
「拆一篇内容、一个账号为什么有效」的创作域内容。
目标：既总结「发生了什么」（同 case），更提炼**可复用的结构模具**——标题公式、开头钩子、
正文节奏、结尾 CTA——让读者换一个主题仍能直接套用到自己的创作上。

# 使用说明
- **输出格式**：纯标准 Markdown，无开场白、无结束语、无解释。
- **风格**：客观、重证据、模具可迁移；单篇正文 800～1500 字。
- **去水分**：原文没讲的模块不硬凑；数据缺就如实说明，不编造数字。

---

# 固定结构（顺序不可颠倒）

1. **标题 + 标签 + 作者**（统一规范，作者取真实名；**来源链接由系统自动追加，禁止在正文写来源链接**）。
2. **一句话核心**：这个案例最该被记住的「为什么有效」（放引用块）。
3. **背景与结果**：账号 / 内容是什么、什么平台、什么阶段；最终数据结果（粉丝 / 播放 / 营收等，尽量给具体数字与时间窗）。
4. **打法拆解**：它实际做了什么——按「选题 → 内容结构 → 发布 / 运营动作」分层罗列关键动作。
5. **可复用结构模具（核心，必备）**：把有效做法抽象成模具，用表格呈现：

   | 模具字段 | 提炼结果 | 原文依据 |
   |---------|---------|---------|
   | 标题公式 | [占位符] 形式，如 [数字]+[痛点]+[利益] | 「原文标题原句」 |
   | 开头钩子 | 类型归类（冲突 / 反常识 / 痛点 / 利益 / 故事）+ 一句做法 | … |
   | 正文节奏 | 段落组织 / 信息密度分布 / 转折点位置 | … |
   | 结尾 CTA | 引导什么行为、怎么引导 | … |
   | 适用场景 | 平台 / 内容形态 / 人群 | … |

   - **标题公式必须占位符化**（用 `[ ]` 标变量），禁止照抄原文标题当公式。
   - 每个字段都要有原文依据支撑；你抽象出的规律标「（笔记者推断）」。
6. **数据与证据**：支撑「为什么有效」的指标链（如 前3秒完播 → 转发 → 转化），保留具体数字。
7. **禁区（避坑清单）**：原文明确指出 / 反面印证的坑，前置 `⚠️`；原文没提则整块省略，不凑。
8. **延伸思考 + 我的想法**：
   ```markdown
   1. <这套模具套到你的账号 / 内容上，哪个字段要先改？>
   2. <换个平台或人群，结论还成立吗？>
   > ✍️ 我的想法：
   > （留白，供你写下自己的套用打算）
   ```
   > 可顺着**思维模型清单**（第一性原理→5-Why冰山→二阶思维→脉络还原→奥卡姆剃刀→类比迁移，见末尾通用规范第九节）重新审视「为什么有效」，每条适用须产出「洞察（不同视角）+ 原文证据句（「」括起原话，禁止改写）」，不准只写「用了X 模型」。
9. **总结收束**：文末用一句**加粗**的话收束全场。

---

# 提炼规范（本模板特有边界）

1. **红线是「不侵权」，不是「不借鉴」**：结构 / 套路 / 玩法人人可学；**微创新——套模具后换主题 / 换平台 / 换人群 / 换表达再产出——是被鼓励的正道**，可在模具表或「延伸思考」给微创新套用建议（标「（笔记者推断）」）。仅两条硬禁：① 把博主**原创文案整段照搬**当自己的作品发布；② 直接**搬运博主独家素材**。引用原句做学习证据（模具表「原文依据」列、金句摘录）属笔记合理引用，不在此限。
2. **占位符铁律**：标题公式用 `[数字]+[痛点]+[利益]` 式占位符表达，保证换主题仍可用；照抄原文标题当公式 = 不合格（「原文依据」列引用原句不受此限）。
3. **素材分级（个案判断，不一刀切）**：博主**独家原创**素材（自绘 / 买断 / 授权图）→ 只描述「用法思路」，自产替代；**大众 / 公开素材**（平台公共模板 / 常识性图表 / 免费图库 / 通用框架）→ **人人可用**，可直接参考其用法与组合方式，如实标注来源即可。
4. 数据与结论必须保留，禁止删减；你的抽象 / 归纳一律标「（笔记者推断）」。
5. 关键词加粗，段落空行分隔，单段不超 4 行；剔除寒暄与营销铺垫。"""

# ---------------------------------------------------------------------------
# 通用强化规范（轻模板 key_points / case / opinion 共享；structured 已内联等价规则）
# ---------------------------------------------------------------------------
# 思维模型透镜（有序 LIST · 按需触发）—— 见 DECISION-20260721-note-quality
THINKING_LENS = (
    "> 目标：用经典思维模型把内容的底层逻辑扒出来，提升总结深度与上下文清晰度。"
    "它不是“每段必套”的工具，而是一张**有序清单**——当内容有值得深挖的非显性机制 / 因果链 / 决策分歧时，"
    "从第一条开始**按顺序逐条过**，每条若适用就产出一个“不同的点”（一个洞察），不适用就跳过。"
    "禁止为“用模型”而硬套——无洞察可挖时整段不写（不违反去水分红线）。\n\n"
    "按顺序调用（顺序本身带来系统性）：\n\n"
    "1. **第一性原理**：把主张 / 方法论拆到不可再分的基本要素，再重组，暴露“为什么必须如此、哪些假设是隐含的”。\n"
    "   - 触发：内容是方法论 / 主张 / 规则时。提问：抛开行业惯例，这件事最底层的要素是什么？\n"
    "2. **5-Why / 冰山模型**：对成败 / 问题连续追问“为什么”，追到根因而非停在表象。\n"
    "   - 触发：内容含成功 / 失败 / 问题 / 异常时。提问：表面原因之下，真正驱动它的第 N 层原因是什么？\n"
    "3. **二阶思维**：不只看这一步的直接后果，追问“后果的后果”。\n"
    "   - 触发：内容涉及决策 / 策略 / 选择时。提问：如果这么做，下一步会引发什么连锁反应？\n"
    "4. **脉络还原（第一手上下文）**：还原“谁、在什么约束下、为什么这么做”，直接服务上下文清晰。\n"
    "   - 触发：任何案例 / 数据 / 结论出现前。提问：这个结论的处境和前提是什么？换处境还成立吗？\n"
    "5. **奥卡姆剃刀**：繁杂内容里只留真正驱动的因素，砍掉多余解释（与去水分一致）。\n"
    "   - 触发：内容信息繁杂 / 多因素时。提问：最少几个因素就能解释现象？哪些是噪音？\n"
    "6. **类比迁移**：用读者熟悉的事物类比抽象概念，降低理解门槛。\n"
    "   - 触发：内容抽象 / 专业门槛高时。提问：这像生活里什么熟悉的东西？\n\n"
    "## 强制证据（红线中的红线）\n"
    "- **每条适用模型必须产出「洞察 + 原文证据句」**：\n"
    "  - 洞察 = 不同于原文的视角 / 追问（如「真正驱动它的不可再分要素是 X」）；\n"
    "  - 证据 = 用「」括起支撑该洞察的**原话片段**（一句即可，禁止改写）。\n"
    "  - ❌ 禁止只写「用了第一性原理」这类空话——那等于没深挖，按「不适用」处理。\n\n"
    "红线：\n"
    "- 只在“值得深挖”处用，不每篇硬塞（不违反不凑数）。\n"
    "- 模型推导须就近标注「（笔记者推断）」；证据句须为原文原话，不改写。\n"
    "- 深度不超过原文支撑，不脑补。"
)

UNIVERSAL_RULES = """# 通用强化规范（适用于一切笔记类型，最高执行标准）

> 本规范与各类笔记模板自身要求同级，必须 100% 遵守，优先级高于一切临时口语指令。

## 一、输出与格式（硬性）
- 输出**纯标准 Markdown**，无开场白、无结束语、无解释、无闲聊。
- 标题层级固定：`#` 一级 / `##` 二级 / `###` 三级；更小标题用 **加粗** 不再加 `#`。
- **H1 必须是内容标题**：用一句有信息量的话概括本篇核心（如「吸金的便士秤：巴菲特 7 岁读到的那台一分钱体重秤如何滚出 70 台生意网络」），禁止把文件名/集号原样抄作 H1（如禁止 `# 第02集_吸金的便士秤...` 这种文件名感标题）。
- **标签行必须用井号格式**：紧跟 H1（无空行），写成 `#标签1 #标签2 #标签3`（多个标签用空格分隔，每个以 `#` 起头）；**禁止**写成 `**标签**：xxx` 或 `标签：xxx` 这种非井号形式（那是错误的元数据格式，解析器无法识别）。
- **段落之间空一行**；单段不超过 5 行，一段只表达一个核心意思。
- 大板块之间用 `---` 分割线隔离。
- 对比 / 参数 / 层级 / 盘点类内容**必须用表格**。

## 二、排版与符号（硬性）
- **关键词 / 定义 / 金句加粗高亮，禁止大面积整段加粗**。
- 金句 / 原文引用用 `>` 引用块包裹，保留原话，禁止改写、转述、翻译。
- 对错用 `✅ / ❌`，风险 / 坑点前置 `⚠️`；外部链接统一 `[文字](url)`。

## 三、案例与背景（最高优先级 · 解决"不连贯 / 缺背景"）
- 凡涉及案例、故事、数据推导，**必须完整保留「上下文背景 → 前因 → 经过细节 → 关键前提 → 后果 / 启示」的完整链条**，不只留结论与零散数字。
- 提炼后**脱离原文也能看懂来龙去脉**：读者没看过原文，也能明白人物是谁、为何这么做、条件是什么、结果如何。

## 四、连贯性（最高优先级）
- 段落之间要有**逻辑衔接**，不堆砌孤立结论；数据 / 论点出现前先交代**前提与来源场景**，禁止突兀抛数字。
- 每条核心论点内部：**先给结论小标题 → 再展开背景与因果 → 最后落到证据 / 数据**。

## 五、可信度标注（防编造混入原意）
- 默认所有陈述视为**原文事实**，如实记录。
- 你根据常识**推断 / 补全**的内容，就近标注「（笔记者推断）」。
- 你额外补充的延伸 / 背景，用「【补充】」前缀，与原文事实区分。

## 六、内容边界（红线，违规即不合格）
- 核心数据、案例、公式、金句、结论**一条不准删**。
- **禁止篡改原文观点、禁止编造作者未表达过的内容、禁止为凑模块编造内容**。
- 可剔除寒暄、自我介绍、无关铺垫，但**不可砍掉承载论点的背景故事**。

## 七、语言风格（统一语感）
- 通俗口语化 + 专业严谨并存；专业名词必须附大白话解释。
- 短句为主，一句话控制在 20 字左右，阅读无压力。
- 文末用一句**加粗**的话收束全场，串联知识体系。

## 八、质量自检红线（杜绝敷衍 · 输出前必过）

输出前，你必须逐条自检，任何一条不达标即判为「敷衍 / 烂输出」，必须重写：

- **禁止标题级空话**：每个核心模块必须有「具体可证伪的结论 + 原文证据/案例/数据支撑 + 适用边界或前提」，不得只有一句正确的废话或一个孤零零的小标题。
- **禁止目录式复述**：不能把原文的目录 / 小标题原样抄成总结骨架——复述 ≠ 总结。总结必须提炼、重组、给出判断。
- **禁止万能概括句**：剔除「要坚持」「要努力」「方法论很重要」「流量为王」这类放之四海皆准、无信息量的话；每条结论都要带具体场景与依据。
- **禁止凑数**：凡是为了「结构完整」而写、原文并无对应信息的内容，一律删除。
- **深度自检**：脱离原文后读者能否看懂来龙去脉？每个结论是否都有支撑？有没有任何一句可以删掉而不损失信息？三者全过才合格。
- **一本正经胡说禁止**：任何推断 / 补全必须标注「（笔记者推断）」或「【补充】」，未标注即视为把编造当事实，违规。"""

# 思维模型自检（提质闭环·强制）：确保「按需深挖」真发生，不破去水分红线
THINKING_SELFCHECK = """## 十、思维模型自检（提质闭环 · 强制）

文末「延伸思考」必须显式回应本透镜清单，二选一，且必须写明：
- **套用了**：对每个适用模型，必须给「洞察 + 原文证据句（「」括起原话）」（如「第一性原理（套用了）：真正驱动 X 的不是 Y，而是 Z 这一不可再分要素；证据：「原文：……」」）。只写「用了第一性原理」而无洞察 / 无证据，按『不适用』处理。
- **全不适用**：必须**逐条列出 6 个模型**（第一性原理 / 5-Why / 二阶思维 / 脉络还原 / 奥卡姆剃刀 / 类比迁移），每条给一句「为何不适用」的理由，再写「思维模型自检：6 个模型逐条过，均不适用」。

禁止的情况：① 只字不提思维模型清单；② 为凑「套了」而生硬给每个模型各写一句废话（无真实洞察 + 无证据即按『不适用』处理）；③ 「全不适用」只写一句笼统话、不逐条列明。
此自检只用于确认「按需深挖」真发生——无洞察可挖的条就跳过，但『跳过』本身必须被逐条说明，不得默认蒙混。"""

# 第九节：思维模型透镜（有序 LIST · 按需触发）
UNIVERSAL_RULES = UNIVERSAL_RULES + "\n\n## 九、思维模型透镜（有序 LIST · 按需触发）\n\n" + THINKING_LENS + "\n\n" + THINKING_SELFCHECK

# 让要点提炼 / 案例拆解 / 观点卡 三个轻模板，补齐 structured 才有的通用规范
KEY_POINTS_PROMPT = KEY_POINTS_PROMPT + "\n\n---\n\n" + UNIVERSAL_RULES
CASE_PROMPT = CASE_PROMPT + "\n\n---\n\n" + UNIVERSAL_RULES
OPINION_PROMPT = OPINION_PROMPT + "\n\n---\n\n" + UNIVERSAL_RULES

# structured 不走轻模板拼接路径，这里内联等价规则，保证 4 模板全覆盖思维模型透镜
CONTENT_SUMMARY_PROMPT = CONTENT_SUMMARY_PROMPT + "\n\n# 十四、思维模型透镜（有序 LIST · 按需触发）\n\n" + THINKING_LENS + "\n\n# 十五、思维模型自检（提质闭环 · 强制）\n\n" + THINKING_SELFCHECK

# 访谈 / 盘点 / 读书 三个新模板：与 key_points/case/opinion 同路径，拼 UNIVERSAL_RULES（含第九节思维模型透镜）
INTERVIEW_PROMPT = INTERVIEW_PROMPT + "\n\n---\n\n" + UNIVERSAL_RULES
ROUNDUP_PROMPT = ROUNDUP_PROMPT + "\n\n---\n\n" + UNIVERSAL_RULES
READING_PROMPT = READING_PROMPT + "\n\n---\n\n" + UNIVERSAL_RULES

# 创作解剖（2026-08-26 新增）：同轻模板路径，拼 UNIVERSAL_RULES
DISSECTION_PROMPT = DISSECTION_PROMPT + "\n\n---\n\n" + UNIVERSAL_RULES

NOTE_TEMPLATES = {
    "structured": {
        "name": "结构化复盘笔记",
        "desc": "学习/课程/教程/方法论类：概念→分维度拆解→正反例→步骤→可复用模板（按需模块，去水分）",
        "prompt": CONTENT_SUMMARY_PROMPT,
    },
    "key_points": {
        "name": "要点提炼笔记",
        "desc": "公开课/讲座/演讲/播客类：核心论点+金句+正反例+行动项+延伸思考",
        "prompt": KEY_POINTS_PROMPT,
    },
    "case": {
        "name": "案例拆解笔记",
        "desc": "案例/复盘/产品拆解类：背景→决策→结果→正反例→可复用经验+延伸思考",
        "prompt": CASE_PROMPT,
    },
    "opinion": {
        "name": "观点卡",
        "desc": "评论/随笔/立场文类：核心立场+论据+正反双方+我的立场",
        "prompt": OPINION_PROMPT,
    },
    "interview": {
        "name": "访谈笔记",
        "desc": "专访/对谈/播客访谈/Q&A类：问答对+嘉宾背景+双方分歧+金句+延伸思考",
        "prompt": INTERVIEW_PROMPT,
    },
    "roundup": {
        "name": "盘点笔记",
        "desc": "N个最佳X/横评/测评/工具对比类：维度×对象矩阵+场景结论+选购建议",
        "prompt": ROUNDUP_PROMPT,
    },
    "reading": {
        "name": "读书笔记",
        "desc": "书评/拆书/读后感/读书笔记类：全书地图+核心论点拆解+金句+行动启发",
        "prompt": READING_PROMPT,
    },
    "dissection": {
        "name": "创作解剖笔记",
        "desc": "爆款拆解/带货/涨粉/账号运营复盘类：背景→打法→结果→可复用结构模具（标题公式/钩子/节奏/CTA 占位符化）",
        "prompt": DISSECTION_PROMPT,
    },
    "general": {
        "name": "通用笔记",
        "desc": "兜底通用形态：无明确特化信号时的默认总结（概念→要点→可复用结论），等价于结构化复盘的通用版",
        "prompt": CONTENT_SUMMARY_PROMPT,
    },
}


def get_note_prompt(note_type: str = "structured") -> str:
    """按笔记类型取对应的总结 Prompt 模板。

    未知类型安全回退到 structured。
    """
    t = NOTE_TEMPLATES.get(note_type)
    if not t:
        t = NOTE_TEMPLATES["structured"]
    return t["prompt"]


def list_note_types():
    """列出所有可用笔记类型（key / 名称 / 说明）。"""
    return [
        {"key": k, "name": v["name"], "desc": v["desc"]}
        for k, v in NOTE_TEMPLATES.items()
    ]


def format_note_with_prompt(content: str, author: str = "", url: str = "", tags: list = None, add_metadata: bool = True, publish_time: int = 0) -> str:
    """格式化笔记内容

    Args:
        content: 笔记正文内容
        author: 作者信息
        url: 原文链接
        tags: 标签列表
        add_metadata: 是否添加元数据（标签、作者、链接）
        publish_time: 内容原始发布时间（epoch 秒）；>0 时在元信息追加「发布时间」

    Returns:
        格式化后的笔记内容
    """
    tags = tags or []

    # F：统一元数据格式（把 `**标签**：xxx` 等非井号格式归一为 `#标签1 #标签2`）
    content = normalize_note_metadata(content)

    if add_metadata:
        # 去除 content 中可能残留的旧标签行（param tags 为权威，避免重复标签行）
        content_lines = [l for l in content.split("\n") if not _TAG_LINE_RE.match(l)]
        content = "\n".join(content_lines)

        # 添加标签
        if tags:
            formatted = "#" + " #".join(tags) + "\n\n"
        else:
            formatted = ""

        # 来源链接 = formatter 单一权威写入（模板已禁止 LLM 在正文写来源链接）。
        # 这样每篇永远只有一个来源链接，且 URL 来自流水线 original_url（抓取真相）。
        # 若 LLM 仍写了作者/嘉宾行，则只补来源链接、避免双作者行。
        author_display = author if author else "【作者未知】"
        has_source_link = "**来源链接**" in content
        has_author_line = ("**作者**" in content or "**嘉宾**" in content
                           or "**书作者**" in content or "**访谈者**" in content)
        if not has_source_link:
            if url:
                if has_author_line:
                    formatted += f"**来源链接**：[原文链接]({url})\n\n"
                else:
                    formatted += f"**作者**：{author_display} | **来源链接**：[原文链接]({url})\n\n"
            else:
                if has_author_line:
                    formatted += "**来源链接**：【无来源链接】\n\n"
                else:
                    formatted += f"**作者**：{author_display} | **来源链接**：【无来源链接】\n\n"

        # 内容原始发布时间（若提供）：让笔记记录「内容何时发的」，而非仅记录我们处理时间
        if publish_time and publish_time > 0:
            pub_date = datetime.fromtimestamp(publish_time).strftime("%Y-%m-%d %H:%M")
            formatted += f"**发布时间**：{pub_date}\n\n"

        formatted += content
    else:
        formatted = content

    return formatted


# ---------------------------------------------------------------------------
# F：元数据格式归一（井号标签）
# ---------------------------------------------------------------------------
_TAG_LINE_RE = re.compile(r'^\s*\*{0,2}标签\*{0,2}\s*[：:]\s*(.+?)\s*$')

def normalize_note_metadata(md: str) -> str:
    """把笔记里错误的标签行（`**标签**：xxx` / `标签：xxx`）归一为井号格式。

    - 识别 `**标签**：访谈笔记 · 创业 · 人生决策` 这类行；
    - 按 逗号/顿号/空格/中圆点/斜杠/竖线 切分为多个标签，每个前缀 `#`；
    - 去重；无标签可解析时丢弃该行（不保留错误格式）。
    - 不改其他元数据行（如 `**嘉宾**` / `**访谈者**`）。
    """
    if not md:
        return md
    lines = md.split("\n")
    out = []
    for line in lines:
        m = _TAG_LINE_RE.match(line)
        if m:
            raw = m.group(1).strip()
            parts = re.split(r'[,，、\s·/｜|]+', raw)
            tags = []
            for p in parts:
                p = p.strip().strip('#').strip()
                if p and p not in tags:
                    tags.append(p)
            if tags:
                out.append("#" + " #".join(tags))
                continue
            # 无可解析标签：丢弃错误格式行
            continue
        out.append(line)
    return "\n".join(out)


# ---------------------------------------------------------------------------
# A：质量闸门（second-pass verifier · 最大提质杠杆）
# ---------------------------------------------------------------------------
# 开关：默认关闭（省一轮 AI 调用）；置 NOTE_QUALITY_GATE=1 可随时开启。
QUALITY_GATE_ENABLED = os.environ.get("NOTE_QUALITY_GATE", "0") == "1"
# 评分阈值：低于此分视为不达标，触发带反馈重试一次。
GATE_THRESHOLD = int(os.environ.get("NOTE_GATE_THRESHOLD", "85"))


QUALITY_GATE_PROMPT = """# 笔记质量审核员（第二遍把关）

你是一位严格的笔记质量审核员。你会拿到【原文/字幕】与【AI 生成的笔记】，
按下方「质量红线」逐条核查，给出 **0–100 分** 的评分与**具体修订清单**。

## 评分维度（任一不过即扣分）
1. **忠于原意**：是否篡改/扭曲作者核心观点？是否编造原文没有的内容（第一红线）？
2. **不敷衍**：是否存在标题级空话、目录式复述、万能概括句、为凑模块编造？
3. **上下文清晰**：脱离原文能否看懂来龙去脉？案例/数据是否带完整背景链条？
4. **思维模型落地**：延伸思考是否真按「思维模型清单」逐条深挖并附**原文证据句（「」括起）**？还是只写「用了X」的空话？
5. **结构合规**：是否 100% 遵守对应笔记模板的固定结构（标签/H1/问答对/对比矩阵等）？
6. **可信度标注**：推断/补全是否标「（笔记者推断）」或「【补充】」？

## 输出格式（严格 JSON，不要任何解释之外的内容）
{
  "score": <0-100 整数>,
  "pass": <true/false，score>=85 为 true>,
  "issues": [
    "<问题1：指明违反哪条红线 + 具体位置/句子 + 该怎么改>",
    "..."
  ]
}
若笔记已达标，issues 可为空数组，但 score 必须如实给（不许滥给高分）。"""

QUALITY_GATE_SELFCHECK = (
    "\n\n# 质量自检闸门（你必须执行）\n"
    "写完后，在上文通用规范（含质量自检红线）之外，补做以下两条专项自检；任一条不达标必须重写，不可交差：\n"
    "① 思维模型清单已逐条深挖并附**原文证据句（「」括起）**（「套用了」或逐条说明不适用，二选一）；\n"
    "② 100% 遵守上方模板固定结构（模块顺序、必备项一项未缺）。\n"
    "若你判断某项不达标，直接修订后再输出最终笔记。"
)


def parse_quality_gate(raw: str) -> dict:
    """解析审核员输出为 {score, passed, issues}（容错：JSON 失败则正则兜底）。"""
    if not raw:
        return {"score": 0, "passed": False, "issues": ["审核输出为空"], "raw": raw}
    text = raw.strip()
    block = None
    m = re.search(r"\{.*\}", text, re.DOTALL)
    if m:
        block = m.group(0)
    try:
        if block:
            data = json.loads(block)
            score = int(data.get("score", 0))
            issues = [str(i) for i in (data.get("issues") or []) if str(i).strip()]
            passed = bool(data.get("pass", score >= GATE_THRESHOLD))
            return {"score": max(0, min(100, score)), "passed": passed, "issues": issues, "raw": raw}
    except Exception:
        pass
    # 兜底正则
    sm = re.search(r"score\"?\s*[:=]\s*(\d+)", text, re.IGNORECASE)
    score = int(sm.group(1)) if sm else 0
    issues = re.findall(r'["\'](.+?)["\']', text)
    issues = [i for i in issues if len(i) > 4 and "score" not in i.lower()]
    return {"score": max(0, min(100, score)), "passed": score >= GATE_THRESHOLD, "issues": issues, "raw": raw}


def verify_note(note: str, source: str = "", note_type: str = "") -> "Optional[dict]":
    """质量闸门：调 AI 审核笔记，返回 {score, passed, issues}；无 AI 返回 None。

    设计：仅作「第二遍把关」，不重写笔记本身；调用方据 passed 决定是否带反馈重试。
    无外部 AI Provider（降级路径）时返回 None，由调用方走自检闸门（QUALITY_GATE_SELFCHECK）。
    """
    if not QUALITY_GATE_ENABLED:
        return None
    if not note or not note.strip():
        return None
    src = (source or "")[:6000]  # 控长度，审核只看代表性原文
    content = f"【原文/字幕（节选）】\n{src}\n\n【AI 生成的笔记】\n{note}"
    # 延迟导入避免循环依赖（articles.main 不在 templates 的导入链上）
    try:
        from articles.main import call_ai_summary_with_meta
    except Exception:
        return None
    meta = call_ai_summary_with_meta(QUALITY_GATE_PROMPT, content, temperature=0.2)
    if not meta or not meta.get("content"):
        return None
    return parse_quality_gate(meta["content"])


def should_gate_retry(gate: "Optional[dict]") -> bool:
    """是否需带反馈重试：闸门开启、未达标、且分数低于阈值。"""
    if not QUALITY_GATE_ENABLED:
        return False
    return bool(gate) and not gate.get("passed", True) and gate.get("score", 100) < GATE_THRESHOLD


def build_gate_critique(gate: dict) -> str:
    """把审核问题拼成可追加到 prompt 的反馈段（限前 6 条）。"""
    if not gate:
        return ""
    issues = gate.get("issues", []) or []
    score = gate.get("score", 0)
    if not issues:
        return (f"\n\n【质量闸门反馈·上一版 {score}/100】请整体再打磨，"
                f"确保 6 条红线全过。")
    lines = "\n".join(f"- {i}" for i in issues[:6])
    return f"\n\n【质量闸门反馈·上一版 {score}/100】请逐条修订：\n{lines}"
