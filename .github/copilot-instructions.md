# GitHub Copilot 指令（可选）

本项目 agent 规则的唯一真源是根目录 `AGENTS.md`（及其配套的 `RULES.md`）。
为避免规则漂移，本文件不重复规则内容，仅指向真源：

- 完整规则：`AGENTS.md` + `RULES.md`
- 如需让 Copilot 加载，可将上述两文件内容复制进本文件，或保持本指针即可。

> 本项目默认使用 WorkBuddy / Cursor / Trae / Codex，均直接读 `AGENTS.md`，无需本文件。
