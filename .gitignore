__pycache__/
*.pyc
.venv/
venv/
# Trae：技能镜像含 .env 密钥不提交；项目规则(.trae/rules)与配置(skill-config.json)需提交
.trae/skills/
.env
# .env 备份（含 cookie/密钥，绝不提交）
.env.bak*
notes/
monitors/state.json
monitors/backfill_targets.json
monitors/.wechat_auth.json
monitors/.weread_cookie
monitors/.login_qr.png
monitors/.login_uuid.json
monitors/subscriptions.json
monitors/login_qr*.svg
monitors/login_qr*.png
monitors/_test_state.json
monitors/*.log
# 门禁拦截台账（运行时数据，7 天滚动自清理）
monitors/gate_blockers.*.jsonl
monitors/.mp_cache.json
monitors/.poll_daemon.pid
monitors/.relogin.lock
monitors/.relogin.mutex
monitors/.run.lock
monitors/pending_refetch.json
monitors/pending_summaries.json
monitors/pending_series.json
monitors/series_state.json
*.bak
stderr.log
stdout.log
.cache/
.pytest_cache/
.workbuddy/
_test_artifacts/
*.egg-info/
nul
dist/
build/
# ASR 转写缓存（transcripts/<id>.md）：可经 videos/asr 自动重生成，不提交
transcripts/
apply_*.log
# 并行监控运行态产物（任务 ledger / 状态快照），非源码，不提交
monitors/run_status/

# 迁移 / 看护 运行时产物（非源码，不提交）
scripts/_*
# 临时 / 原型 scratch 目录（分类器原型、index CSV 快照、诊断日志、临时落盘脚本等，非源码）
_tmp/
scripts/feishu_to_obsidian_log.json
scripts/migrate_watchdog.log
scripts/migrate_watchdog_status.json
scripts/migration_obsidian_log.json
# B站 Cookie 轮换运行时状态（现行机制使用，非源码）
scripts/bili_cookie_rotation.json


# 个人材料（简历/面试），不入库
interview_prep/
